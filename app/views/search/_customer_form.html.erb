<%= form_with url: customers_search_path, method: :get, local: true do |f| %>
  <%= f.text_field :content, placeholder: "ユーザー検索" %>
  <%= f.select :method, options_for_select({ "完全一致" => "perfect", "前方一致" => "forward", "後方一致" => "backward", "部分一致" => "partial" }) %>
  <%= f.submit '検索' %>
<% end %>