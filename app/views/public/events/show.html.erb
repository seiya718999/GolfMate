<div class="container pt-5">
  <div class="row">
    <div class="col-md-4">
      <p>
        <strong>日時</strong>
        <%= @event.date.strftime("%m-%d") %>
      </p>
      <p>
        <strong>予定</strong>
        <%= @event.schedule_i18n %>
      </p>
      
      <p>
        <strong>詳細</strong>
        <%= @event.description %>
      </p>

<%= link_to '編集', edit_event_path(@event) %>
<%= link_to '消去',event_path(@event.id), method: :delete, data: { confirm: '本当に?' } %>
<%= link_to '戻る', events_path %>
    </div>
  </div>
</div>